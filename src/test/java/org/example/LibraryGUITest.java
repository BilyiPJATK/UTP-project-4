package org.example;

import org.junit.Test;

import static org.junit.Assert.assertTrue;
import static org.junit.jupiter.api.Assertions.*;

class LibraryGUITest {
    @Test
    void testtrt() {

        LibraryGUI dialogUtils = mock(DialogUtils.class);

         LibraryGUI.createBookForm();
        assertTrue();
    }
}